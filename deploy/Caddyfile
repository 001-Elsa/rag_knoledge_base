{$DOMAIN::80} {
    encode gzip

    # Operational endpoints are scraped on the internal Compose network only.
    @internal path /metrics
    respond @internal 404

    reverse_proxy api:8000 {
        flush_interval -1
    }
}
